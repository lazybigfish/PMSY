每次提出需求调整后，首先要做的是更新需求文档、然后让用户确认需求文档是否正确、最后再根据用户确认的需求文档进行调整，需求文档放在当天日期的目录下
每次功能做完以后，要先及时自测功能是否正常
项目上一定要有一个单独的测试目录，用来放置所有的测试用例、测试数据、测试脚本、测试结果等
UI 库： 指定使用 TailwindCSS
超过 60 行的代码优先抽象为函数或模块
每次写组件的时候，优先考虑是否可以抽象为函数或模块，避免重复代码，同时先找一下项目内是否已经有类似的组件或函数，如果有，就优先考虑复用已有的组件或函数，而不是重复编写相同的代码
避免不必要的对象复制；减少多层嵌套，采用提前返回；使用线程安全的并发控制机制。

- 每次修复问题后，必须更新 .agent/records/当天日期/项目开发日报.md